require 'rails_helper'

RSpec.describe AppVersion, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:app_version)
  #   expect(record).to be_persisted
  # end
end
